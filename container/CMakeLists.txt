#========================================================================= 
set(myarray
    myarray.h
)

add_library(myarray ${myarray})

#========================================================================= 
set(myoptional
    myoptional.h
)

add_library(myoptional ${myoptional})